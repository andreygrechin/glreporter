{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "make build",
            "group": "build",
            "problemMatcher": [
                "$go"
            ]
        }
    ]
}
